import SiteNav from '../nav';
import {sitePath} from '../site-path';
const boards=[{title:'Pan–Tilt Control',visual:'CONTROL + SERVO POWER',copy:'Power, two servo outputs and the controller interface in one clean board.',needs:['2× servo outputs','Power regulation','Control interface','Serial access']},{title:'Steady-Spoon Controller',visual:'SENSING + ACTUATION',copy:'Sensing, actuation and battery power in a compact product-shaped layout.',needs:['Sensor inputs','Actuator drive','Battery path','Compact footprint']}];
export default function Pcb(){return <main className="screen"><SiteNav active="pcb"/><section className="sectionScreen"><header className="compactHead"><div><span>ELECTRONICS / PLANNED</span><h1>PCB</h1></div><p>Requirements now. Board layouts next.</p></header><div className="twoPanel boardPanels">{boards.map(board=><article key={board.title}><div className="miniBoard"><strong>{board.visual}</strong><i/><i/><b/><b/></div><header><small>PLANNED</small><h2>{board.title}</h2></header><p>{board.copy}</p><div className="chipRow">{board.needs.map(need=><span key={need}>{need}</span>)}</div></article>)}</div></section><footer className="minimalFooter"><a href={sitePath('/')}>← HOME</a><span>NO FINISHED BOARDS CLAIMED</span></footer></main>}

